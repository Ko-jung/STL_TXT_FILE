

============================================================
소스.cpp, 저장시간: 04/13/23 Thursday 16:14:13 
============================================================
//--------------------------------------------------------------------
// 2023년 1학기 STL 3월 2일 목78		화910 목78		(7주 1일)
//--------------------------------------------------------------------
// Sequence Container - vector
//--------------------------------------------------------------------

#include <iostream>
#include <vector>
#include <fstream>
#include <array>

#include "String.h"
#include "save.h"

using namespace std;

// [문제] "소스.cpp"에 있는 영문자의 개수(대소문자 구분없음)를 다음과 같이 출력하라
// a - 10
// b - 3
// ...
// ...
// z - 1
//-----------
int main()
//-----------
{
	ifstream in{ "소스.cpp" };
	// ----------------------------professor----------------------------
	array<int, 26> a{};
	if (!in)
		return 0;

	char c;
	while (in >> c) {
		if (isalpha(c))
			++a[tolower(c)-'a'];
	}

	for (int i{ 0 }; i < a.size(); ++i)
		cout << char('a' + i) << " - " << a[i] << endl;

	//// ----------------------------mine----------------------------
	//vector<int> v(26, 0);
	////v.reserve(26);

	//char c;
	//while (!in) {
	//	in >> c;
	//	v[c - 'a'] += 1;
	//}

	//for (int i = 0; i < 26; i++) {
	//	cout << char(i + 'a') << " - " << v[i] << endl;
	//}
	// 
	save("소스.cpp");
}